export default function BookDetails () {

    // see Boats Lesson
    
    // import data from Books component (props)

    // pass down data for the specified Book id(isbn#)

    // this component loads onClick() button from Book component

    // identify all book details to pull

    // make elements for all details

    // Render all data

    // Close window or back button

    // Needs unique url ex. hardcover-fiction/isbn
    
    
    return (
        
    )
}